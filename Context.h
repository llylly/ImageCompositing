#ifndef CONTEXT_H
#define CONTEXT_H

#include <string>

using namespace std;

class Context {
public:
    static string projectName;
    static int defaultWindowHeight;
    static int defaultWindowWidth;
    static int defaultImageHeight;
    static int defaultImageWidth;
};

#endif // CONTEXT_H
